---
kind: pipeline
type: kubernetes
name: default

steps:
- name: test
  image: docker:stable
  volumes:
  - name: dockersock
    path: /var/run
  commands:
  - sleep 5 # give docker enough time to start
  - docker ps -a

services:
- name: docker
  image: docker:stable
  
  volumes:
  - name: dockersock
    path: /var/run

volumes:
- name: dockersock
  temp: {}